@model IEnumerable<PaginaPessoal2.Models.Habilitacoes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ano)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Curso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instituicao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ano)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Instituicao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HabilitacoesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HabilitacoesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HabilitacoesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
